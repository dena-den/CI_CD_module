pipeline {
   agent any


   stages {
      stage('Adding_path') {
        steps {
          echo 'Path'
        }
      }
      stage('Creating_environment') {
        steps{
          echo 'env'
        }
        }
   
   stage('Runing_tests') {
     steps {
        bat(".venv\\Scripts\\pytest.exe -v")
     }
   }
   stage('Result') {
      steps {
        echo 'Success!'
      }
   }
  }
}
