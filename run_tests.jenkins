pipeline {
   agent any
   stages {
      stage('Adding_path') {
        steps {
          echo 'Path'
        }
      }
      stage('Creating_environment') {
        steps{
          bat("C:\\1PROGRAMS\\Python\\python.exe -m venv .venv")
          bat(".venv\\Scripts\\activate")
          bat("C:\\1PROGRAMS\\Python\\python.exe -m pip install pytest")
          bat("C:\\1PROGRAMS\\Python\\python.exe -m pip install pyodbc")
          bat("pytest -v")
        }
        }

   stage('Runing_tests') {
     steps {
        echo 'Pass'
     }
   }
   stage('Result') {
      steps {
        echo 'Success!'
      }
   }
  }
}