pipeline {
   agent any


   stages {
      stage('Adding_path') {
        steps {
          bat("set PATH=C:\\1PROGRAMS\\Python\\;%PATH%")
        }
      }
      stage('Creating_environment') {
        steps{
          bat(".venv\\Scripts\\activate")
          bat("pytest -v")
        }
        }
   
   stage('Runing_tests') {
     steps {
        bat("echo 'Success!'")
     }
   }
   stage('Result') {
      steps {
        bat("echo 'Success!'")
      }
   }
  }
}
