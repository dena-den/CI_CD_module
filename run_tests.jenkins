pipeline {
   agent any


   stages {
      stage('Adding_path') {
        steps {
          bat("set PATH=C:\\1PROGRAMS\\Python\\;%PATH%")
        }
      }
      stage('Creating_environment') {
        steps{
          bat(".venv\\Scripts\\activate")
        }
        }
   
   stage('Runing_tests') {
     steps {
        bat("pytest -v")
     }
   }
   stage('Result') {
      steps {
        echo 'Success!'
      }
   }
  }
}
