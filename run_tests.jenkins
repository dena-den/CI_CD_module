pipeline {
   agent any

   stages {
      stage('Build') {
        steps {
          echo 'Building...'
          echo "Running ${env.BUILD_ID} ${env.BUILD_DISPLAY_NAME} on ${env.NODE_NAME} and JOB ${env.JOB_NAME}"
        }
      }
      stage('List') {
        steps{
          sh("pwd")
          sh("ls -a")
          sh("echo ${JENKINS_HOME}")
          sh("${JENKINS_HOME}/workspace/test/.venv/Scripts/activate")
          sh("pytest -v")
        }


        }

   stage('Test') {
     steps {
        echo 'Testing...'
     }
   }
   stage('Deploy') {
     steps {
       echo 'Deploying...'
     }
   }
  }
} 
