pipeline {
   agent any


   stages {
      stage('Adding_path') {
        steps {
          bat("set PATH=C:\\1PROGRAMS\\Python\\;%PATH%")
        }
      }
      stage('Creating_environment') {
        steps{      
          bat("C:\\1PROGRAMS\\Python\\python.exe -m venv .venv")
          bat(".venv\\Scripts\\activate")
          bat("C:\\Users\\Denis_Bulychev\\AppData\\Local\\Microsoft\\WindowsApps\\pip.exe install -r requirements.txt")
        }
        }
   
   stage('Runing_tests') {
     steps {
        bat("pytest -v")
     }
   }
   stage('Result') {
      steps {
        bat("echo 'Success!'")
      }
   }
  }
}
